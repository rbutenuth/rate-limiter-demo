# Rate-limiter Demo Module

This is a Mule demo project for the module [rate-limiter](https://github.com/rbutenuth/rate-limiter/).

Additionally, it contains MUnit tests. Unfortunately it is no longer possible to bundle tests containing flows
with the module project. So this is a workaround to have some automated tests.
